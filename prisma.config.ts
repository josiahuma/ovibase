// prisma.config.ts
// Keep this file compatible with Prisma v5 so Next.js can compile the repo.
// Prisma v5 does NOT support `prisma/config` / `defineConfig`.
//
// Prisma CLI will still use prisma/schema.prisma normally.

const config = {
  schema: "prisma/schema.prisma",
};

export default config;
